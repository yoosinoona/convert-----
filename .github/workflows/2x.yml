name: Convert 2xParagonSR_Nano_gan (Auto-Fix Param)

on:
  workflow_dispatch:

jobs:
  convert:
    runs-on: ubuntu-22.04

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: Install Python deps
        run: pip install onnx onnxsim numpy

      - name: Download ONNX model
        run: |
          wget -q -O net_g_75000_fused_op18_fp32.onnx \
            "https://github.com/Phhofm/models/releases/download/2xParagonSR_Nano_gan/2xParagonSR_Nano_gan_op18_fp32.onnx"
          wget -q -O net_g_75000_fused_op18_fp32.onnx.data \
            "https://github.com/Phhofm/models/releases/download/2xParagonSR_Nano_gan/2xParagonSR_Nano_gan_op18_fp32.onnx.data"
          ls -lh net_g_75000_fused_op18_fp32.onnx net_g_75000_fused_op18_fp32.onnx.data

      - name: Merge external data & decompose Mish
        run: |
          python3 << 'PYEOF'
          import onnx
          from onnx import helper
          import os

          print("Loading ONNX with external data...")
          model = onnx.load("net_g_75000_fused_op18_fp32.onnx")
          print("External data merged OK")

          new_nodes = []
          mish_count = 0
          for node in model.graph.node:
              if node.op_type == "Mish":
                  mish_count += 1
                  x  = node.input[0]
                  y  = node.output[0]
                  sp = y + "_softplus"
                  th = y + "_tanh"
                  new_nodes.append(helper.make_node("Softplus", [x],     [sp]))
                  new_nodes.append(helper.make_node("Tanh",     [sp],    [th]))
                  new_nodes.append(helper.make_node("Mul",      [x, th], [y]))
              else:
                  new_nodes.append(node)

          del model.graph.node[:]
          model.graph.node.extend(new_nodes)
          print(f"Replaced {mish_count} Mish nodes")

          onnx.save(model, "merged.onnx", save_as_external_data=False)
          size_mb = os.path.getsize("merged.onnx") / (1024 * 1024)
          print(f"Saved merged.onnx ({size_mb:.1f} MB)")
          PYEOF

      - name: Simplify ONNX
        run: |
          python -m onnxsim merged.onnx model_simplified.onnx \
            --input-shape "input:1,3,256,256" 2>&1 \
            || cp merged.onnx model_simplified.onnx
          ls -lh model_simplified.onnx

      - name: Download ncnn tools (20240820) & install deps
        run: |
          wget -q "https://github.com/Tencent/ncnn/releases/download/20240820/ncnn-20240820-ubuntu-2204.zip"
          unzip -q ncnn-20240820-ubuntu-2204.zip
          chmod +x ncnn-20240820-ubuntu-2204/bin/*

          wget -q "http://archive.ubuntu.com/ubuntu/pool/main/p/protobuf/libprotobuf23_3.12.4-1ubuntu7_amd64.deb"
          sudo dpkg -i libprotobuf23_3.12.4-1ubuntu7_amd64.deb || true

      - name: Convert ONNX to NCNN
        run: |
          ONNX2NCNN=./ncnn-20240820-ubuntu-2204/bin/onnx2ncnn
          $ONNX2NCNN model_simplified.onnx raw.param raw.bin 2>&1

          if [ ! -f "raw.param" ] || [ ! -f "raw.bin" ]; then
            echo "ERROR: onnx2ncnn failed!"
            exit 1
          fi
          echo "=== onnx2ncnn OK ==="
          ls -lh raw.param raw.bin

      - name: Auto-Fix NCNN Param (Split+Crop -> Slice)
        run: |
          python3 << 'PYEOF'
          import re

          def fix_ncnn_param(input_file, output_file):
              with open(input_file, 'r') as f:
                  lines = f.readlines()

              header = lines[0].strip().split()
              magic = header[0]
              layer_count = int(header[1])
              blob_count = header[2]

              # Tìm tất cả node Split và Crop
              split_map = {}   # output_blob -> input_blob
              split_lines = {} # line_index -> line_content
              crop_lines = {}  # line_index -> info_dict

              for i, line in enumerate(lines):
                  parts = line.strip().split()
                  if len(parts) < 2: continue
                  
                  op_type = parts[0]
                  
                  if op_type == 'Split':
                      num_outputs = int(parts[3])
                      input_blob = parts[4]
                      output_blobs = parts[5:5+num_outputs]
                      for ob in output_blobs:
                          split_map[ob] = input_blob
                      split_lines[i] = line
                      
                  elif op_type == 'Crop':
                      # Tìm pattern động: -23309=1,X -23310=1,Y -23311=1,0
                      match = re.search(r'-23309=1,(\d+)\s+-23310=1,(\d+)\s+-23311=1,0', line)
                      if match:
                          node_name = parts[1]
                          input_blob = parts[4]
                          output_blob = parts[5]
                          start_ch = match.group(1)
                          end_ch = match.group(2)
                          crop_lines[i] = {
                              'name': node_name,
                              'input': input_blob,
                              'output': output_blob,
                              'start': start_ch,
                              'end': end_ch
                          }

              # Xác định các node Split cần xóa (chỉ khi tất cả output của nó đều đi vào Crop lỗi)
              lines_to_remove = set()
              removed_splits = 0
              
              for idx, line in split_lines.items():
                  parts = line.strip().split()
                  num_outputs = int(parts[3])
                  output_blobs = parts[5:5+num_outputs]
                  
                  can_remove = True
                  for ob in output_blobs:
                      # Kiểm tra xem output này có được dùng bởi Crop lỗi không
                      is_used_by_our_crop = False
                      for crop_info in crop_lines.values():
                          if crop_info['input'] == ob:
                              is_used_by_our_crop = True
                              break
                      if not is_used_by_our_crop:
                          can_remove = False
                          break
                  
                  if can_remove:
                      lines_to_remove.add(idx)
                      removed_splits += 1

              # Ghi file mới
              new_layer_count = layer_count - removed_splits
              new_lines = [f"{magic} {new_layer_count} {blob_count}\n"]
              
              replaced_crops = 0
              for i, line in enumerate(lines):
                  if i == 0: continue # Bỏ header cũ
                  
                  if i in lines_to_remove:
                      continue # Xóa node Split
                      
                  if i in crop_lines:
                      crop_info = crop_lines[i]
                      # Nối thẳng Crop về lại tensor gốc trước Split
                      original_input = split_map.get(crop_info['input'], crop_info['input'])
                      # Chuyển Crop thành Slice tĩnh
                      new_line = f"Slice            {crop_info['name']} 1 1 {original_input} {crop_info['output']} -23300=1,{crop_info['start']} -23301=1,{crop_info['end']} -23302=1,0\n"
                      new_lines.append(new_line)
                      replaced_crops += 1
                  else:
                      new_lines.append(line)

              with open(output_file, 'w') as f:
                  f.writelines(new_lines)
                  
              print(f"✅ Auto-Fixed Param: Removed {removed_splits} Split nodes, replaced {replaced_crops} Crop nodes with Slice nodes.")

          fix_ncnn_param('raw.param', '2xParagonSR_Nano_gan.param')
          PYEOF

      - name: Post-process & final check
        run: |
          cp raw.bin 2xParagonSR_Nano_gan.bin

          # DepthToSpace → PixelShuffle
          if grep -q "DepthToSpace" 2xParagonSR_Nano_gan.param; then
            echo "Replacing DepthToSpace → PixelShuffle..."
            sed -i 's/DepthToSpace/PixelShuffle/g' 2xParagonSR_Nano_gan.param
          fi

          echo "=== Final Param Validation ==="
          head -3 2xParagonSR_Nano_gan.param
          
          # Đảm bảo không còn Crop lỗi
          if grep -q "Crop.*\-23309" 2xParagonSR_Nano_gan.param; then
            echo "⚠️ WARNING: File still contains dynamic Crop!"
          else
            echo "✅ Model is completely clean from dynamic Crop/Split!"
          fi

          echo "Slice layers: $(grep -c 'Slice' 2xParagonSR_Nano_gan.param || echo 0)"
          echo "Crop layers: $(grep -c 'Crop' 2xParagonSR_Nano_gan.param || echo 0)"

          BIN_SIZE=$(stat -c%s "2xParagonSR_Nano_gan.bin")
          if [ "$BIN_SIZE" -lt 50000 ]; then
            echo "ERROR: .bin too small!"
            exit 1
          fi

          echo ""
          echo "=== CONVERSION SUCCESS ==="
          ls -lh 2xParagonSR_Nano_gan.param 2xParagonSR_Nano_gan.bin

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: 2xParagonSR_Nano_gan_ncnn_autofixed
          path: |
            2xParagonSR_Nano_gan.param
            2xParagonSR_Nano_gan.bin
          retention-days: 90
